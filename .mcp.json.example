{
  "$comment": [
    "이 파일을 .mcp.json 으로 복사해서 쓴다. .mcp.json 은 .gitignore 대상이다 —",
    "토큰과 로컬 경로가 들어가기 때문이다.",
    "",
    "주의: 원본 저장소에서 .mcp.json 이 gitignore 대상인데 로컬에도 생성되지 않아",
    "슬래시 커맨드 3개가 조용히 동작 불능 상태였다. 복사한 뒤 실제로 서버가 뜨는지",
    "확인하고 넘어간다.",
    "",
    "워크플로우 코어는 MCP 서버 없이 동작한다. 아래는 외부 트래커나 문서 소스를",
    "붙일 때의 예시다. 필요 없으면 mcpServers 를 빈 객체로 둔다."
  ],
  "mcpServers": {}
}
