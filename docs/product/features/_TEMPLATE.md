<!--
  기능 요구사항 템플릿.

  이 문서 하나가 tasks.json 의 태스크 여러 건이 된다.
  §1-2 태스크 분리 표가 그 변환의 입력이며, 이 템플릿에서 가장 중요한 부분이다.

  사용법:
    cp docs/product/features/_TEMPLATE.md docs/product/features/<케밥-이름>.md
    또는 /wf-feature <이름>

  ★ 채우는 순서는 문서 순서와 다르다.
    §2 화면 설계 → §3 API → §5 완료 조건 → 그리고 마지막에 §1-2 태스크 분리.
    §1-2 가 맨 앞에 있는 것은 추출기와 읽는 사람을 위한 배치이지, 먼저 정하라는
    뜻이 아니다. 무엇을 만들지 모르는 채로 쪼개면 나중에 다시 쪼개게 된다.

  분량 기준(원본 저장소 35개 문서 실측): 소형 30~90줄 / 중형 180~290줄 / 대형 430~550줄.
  작은 기능을 대형 문서로 부풀리지 않는다.
-->

# [역할] 기능 이름

**상태**: 초안 <!-- 초안 | 확정 | 구현 중 | 완료 -->
**최종 갱신**: YYYY-MM-DD

---

## 1. 요구사항

### 1-1. 배경

<!--
  지금 무엇이 문제이고 왜 지금 해야 하는가. 4~6줄.
  AS-IS / TO-BE 대비가 명확하면 표로 써도 좋다.
-->

### 1-2. 태스크 분리 (필수)

<!--
  ★ 이 표가 tasks.json 의 태스크가 된다. /wf-tasks-from-doc 이 여기만 읽는다.

  ★ 맨 앞에 있지만 가장 나중에 채운다. §2~§5 를 먼저 쓴다.

  규칙:
  - 백엔드(API)와 프론트엔드(화면)는 항상 다른 행. 한 태스크에 묶지 않는다.
  - FE 행은 "선행"에 BE 태스크를 반드시 적는다.
  - 각 행은 4~16시간 규모여야 한다 (→ docs/workflow/task-schema.md §1).
    4시간 미만이면 인접 행과 합치고, 16시간을 넘으면 나눈다.
  - "포함하지 않음"을 비워두지 않는다 — 범위가 새는 지점이다.
  - 태스크 ID 열은 비워둔다. 추출 후 자동으로 채워진다.
-->

| Task | 범위 | 산출물 | 포함하지 않음 | 선행 | 추정 | 태스크 ID |
|---|---|---|---|---|---|---|
| A. 백엔드 — <무엇> | <어디까지> | <API/스키마> | <이 태스크가 안 하는 것> | — | 8h | |
| B. 프론트 — <무엇> | <어디까지> | <화면/컴포넌트> | <안 하는 것> | A | 6h | |

**분리 이유**: <!-- 왜 이렇게 쪼갰는지 한두 문장. 나중에 되짚을 때 필요하다. -->

```
흐름
  A(백엔드 API) ──► B(프론트 화면)
```

### 1-3. 상세

<!--
  API별 / 데이터별 상세. 필요한 만큼 1-4, 1-5 로 늘린다.
  화면 상세는 §2 로 간다.
-->

---

## 2. 화면 설계

<!--
  ★ 화면이 있을 때만 채운다. 백엔드 전용 기능이면 이 섹션째 지운다.

  ★ 이 섹션이 §3 API 명세와 §5 완료 조건을 결정한다. 먼저 쓴다.
    반대로 하면 — API 를 먼저 정하고 화면을 맞추면 — 화면에 필요한 필드가
    데이터 모델에 없는 채로 구현까지 간다.

  색·타이포·의미 매핑은 여기서 정하지 않는다. 정본은 docs/product/design.md 다.
  기능 문서마다 다시 정하면 화면마다 달라진다.
-->

**UI 라이브러리**: <!-- CLAUDE.md 에 적힌 것. 없으면 정하고 ADR 로 남긴다 -->
**디자인 정본**: `docs/product/design.md` · `design-tokens.css`
**와이어프레임**: <!-- Artifact / Figma 링크 / 없음 — 아래 2-4 로 대신할 수 있다 -->

### 2-1. 이 화면에서 사용자가 하려는 일

<!--
  한 문장. 화면 이름이 아니라 사용자의 목적을 쓴다.
    ❌ 공연 목록 화면
    ✅ 지금 예매할 수 있는 공연이 무엇인지 연락 없이 확인한다

  이 문장이 2-3 의 우선순위를 정한다.
-->

### 2-2. 참조

<!--
  ★ 비슷한 상용 서비스를 최소 하나 열고, 이 화면에 해당하는 부분을 실제로 본다.

    도메인을 모르면 무엇이 빠졌는지 모른다. 이 절이 없으면 "카드에 제목·날짜"
    같은 결론을 상상으로 쓰게 되고, 그 결론이 그대로 데이터 모델까지 간다.

  볼 것:
    - 무엇이 가장 크게 보이나 (그게 그 도메인에서 사용자가 먼저 보는 것이다)
    - 우리 데이터에 없는 것은 무엇인가  ← 이 열이 이 표의 존재 이유다
    - 우리가 안 할 것은 무엇인가 (§7 범위 외로 간다)

  참조가 없는 도메인이면 그렇게 적는다. "없음"과 "안 찾아봄"은 다르다.
-->

| 서비스 | 이 화면에 해당하는 부분 | 우리에게 없는 것 |
|---|---|---|
| <!-- 예: NOL --> | <!-- 지역 추천 목록 --> | <!-- 포스터 이미지, 지역 필터 --> |

**대조 결과**: <!-- 없는 것 중 이번에 넣을 것 / 범위 외로 둘 것을 한두 문장으로 -->

### 2-3. 담기는 것

<!--
  ★ 화면에 놓이는 요소를 우선순위 순으로. 「데이터 출처」가 비어 있으면
    그것은 새 필드이고, §3 API 명세와 마이그레이션이 필요하다는 신호다.

    이 열이 "만들고 보니 이 필드가 없다"를 막는 유일한 장치다.
-->

| 우선 | 요소 | 왜 필요한가 | 데이터 출처 |
|---|---|---|---|
| 1 | <!-- 포스터 --> | <!-- 공연을 식별하는 1차 수단 --> | <!-- ⚠ 없음 — performances.poster_url 신규 --> |
| 2 | <!-- 공연명 --> | <!-- --> | <!-- performances.title --> |
| 3 | <!-- 예매 상태 --> | <!-- 2-1 의 목적 그 자체 --> | <!-- 계산값 (§3 참조) --> |

### 2-4. 배치

<!--
  ASCII 와이어프레임. 무엇이 어디에 얼마나 크게 놓이는지만 그린다.
  색·서체는 그리지 않는다 — design.md 소관이다.

  세로 목록과 그리드는 다른 것을 뜻한다:
    세로 행 목록 = 처리할 항목 (받은편지함, 할 일)
    그리드      = 고르는 상품 (공연, 상품, 사진)
  2-1 의 목적에 맞는 쪽을 고르고, 왜 그쪽인지 한 줄 적는다.
-->

```
┌─────────────────────────────────────────────┐
│  <필터 / 탐색 축>                            │
├──────────┬──────────┬──────────┬────────────┤
│ [포스터] │ [포스터] │ [포스터] │ [포스터]   │
│  공연명  │  공연명  │  공연명  │  공연명    │
│  장소    │  장소    │  장소    │  장소      │
│  날짜    │  날짜    │  날짜    │  날짜      │
└──────────┴──────────┴──────────┴────────────┘
```

**이 배치인 이유**: <!-- 한 줄 -->

### 2-5. 상태별로 무엇이 보이나

<!--
  ★ 각 상태마다 화면에 무엇이 있는지 적는다.
    "예쁘게"가 아니라 "무엇이 보이는가"를 쓴다 — 생김새는 UI 라이브러리와
    design.md 가 정한다.

  빠뜨리기 쉬운 상태:
    빈 목록 · 로딩 · 오류 · 권한 없음 · 아주 긴 텍스트 · 항목이 아주 많을 때
    그리고 2-3 의 선택 요소가 없을 때 (포스터 없는 공연 등)
-->

| 화면 | 상태 |
|---|---|
| <!-- 공연 목록 --> | <!-- 기본 · 로딩 · 빈 목록 · 오류 --> |

- **기본** — <!-- -->
- **로딩** — <!-- -->
- **빈 목록** — <!-- 다음 행동을 제시한다. 빈 화면은 초대다 -->
- **오류** — <!-- 무엇이 잘못됐고 어떻게 하면 되는지. 사과하지 않는다 -->

**이 상태 목록이 그대로 §5 완료 조건이 된다.** 적어놓고 완료 조건에 없으면 검증되지 않는다.

**2-3 의 「데이터 출처」가 그대로 §3 API 명세가 된다.**

---

## 3. 데이터·API 명세

<!--
  DDL 변경분, 요청/응답 DTO, 인덱스, 타입 정의.
  코드 블록으로 실제 형태를 쓴다. 설명만 쓰면 구현에서 다시 결정하게 된다.

  ★ §2-3 에서 「데이터 출처」가 비어 있던 요소가 여기 신규 필드로 나타나야 한다.
    2-3 에 있는데 여기 없으면 화면을 만들 수 없다.
-->

---

## 4. 흐름

<!--
  ASCII 다이어그램으로 쓴다. mermaid·이미지를 쓰지 않는 이유는 이 문서가
  grep 되고 LLM 에 읽히기 위한 것이기 때문이다.
-->

```
사용자 ──► API ──► 서비스 ──► 리포지토리 ──► DB
                     │
                     └──► 이벤트 발행
```

---

## 5. 완료 조건

<!--
  ★ Task 별 체크박스로 쓴다. 그대로 tasks.json 의 acceptance_criteria 가 된다.
  각 항목은 "어떻게 확인하는가"가 명확해야 한다 — 측정 수단이 없는 기준은
  Phase 4 에서 WARN 으로 남는다.

  화면 작업의 완료 조건은 쓰기 어렵다. "시안과 일치한다"는 자동으로 확인할 수 없고,
  "버튼이 파란색이다"는 확인은 되지만 아무 의미가 없다. 동작으로 쓴다:

    ❌ 디자인 시안과 일치한다
    ❌ 버튼이 파란색이다
    ✅ 결과가 0건이면 "담당 매장 중 일치하는 곳이 없습니다" 가 표시된다
    ✅ 403 응답을 받으면 권한 안내로 바뀐다

  §2-5 의 상태 목록이 그대로 체크박스가 된다.
-->

### Task A (백엔드)

- [ ] <관찰 가능한 조건>
- [ ] <관찰 가능한 조건>

### Task B (프론트)

- [ ] <관찰 가능한 조건>

---

## 6. 구현 참고

<!--
  ★ 실제 파일 경로·클래스·함수·엔드포인트를 그대로 인용한다.

  이 섹션이 태스크 추출의 품질을 결정한다. "매장 서비스를 수정한다"라고 쓰면
  추출기가 코드를 찾지 못하고, "src/services/store.py 의 StoreService.search()"
  라고 쓰면 근거로 승격된다.

  아직 없는 파일이면 그렇게 표시한다 (신규: src/api/store_search.py).
-->

- `src/api/store_search.py` — 검색 엔드포인트 (신규)
- `src/services/store.py:StoreService.search()` — 확장 대상
- `GET /api/stores/search` — 신규 엔드포인트

---

## 7. 범위 외

<!--
  ★ 필수 섹션. 이번에 하지 않는 것을 명시한다.
  "나중에 할 수도 있는 것"과 "하지 않기로 한 것"을 구분해서 쓴다.

  §2-2 참조 대조에서 나온 것 중 안 하기로 한 것을 여기 적는다.
-->

- <하지 않는 것> — <이유>

---

## 8. 주요 결정 사항

<!--
  이 기능을 만들며 내린 기술적 결정.

  각 결정에 대해 물어본다: "이 결정이 이 기능을 넘어 다른 곳에도 적용되는가?"
  그렇다면 여기 두지 말고 /wf-adr 로 ADR 을 만든다 — 기능 문서에 갇힌 결정은
  기능이 끝나면 아무도 찾지 못한다.
-->

| 결정 | 이유 | 범위 | ADR |
|---|---|---|---|
| <무엇을 정했나> | <왜> | 이 기능만 / **전체** | — 또는 ADR-0003 |
