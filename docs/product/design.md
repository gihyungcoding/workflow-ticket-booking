# 디자인 방향

<!--
  화면이 있는 프로젝트만 쓴다. 백엔드 전용이면 이 파일을 지운다.

  이 문서는 "어떻게 보이는가"를 결정하고 그 근거를 남긴다.
  architecture.md 가 구조에 대해 하는 일을 디자인에 대해 한다.

  ★ 이 문서의 존재 이유
    "AI 티가 난다"는 취향 문제가 아니라 아무도 결정을 내리지 않은 상태다.
    라이브러리 기본값을 그대로 쓰면 주제와 무관하게 같은 화면이 나온다.
    한 번 의도적으로 정해서 여기 적어두면 그 문제의 대부분이 사라진다.

  작성: /wf-init 2.5 절. frontend-design 스킬의 지침을 따른다.
  검사: 여기서 정한 것 중 기계로 확인 가능한 것을 constraints.yaml 의 DESIGN-* 로 옮긴다.
-->

**최종 갱신**: <!-- TODO: YYYY-MM-DD -->

---

## 1. 이 제품은 무엇인가 (디자인 관점)

<!-- TODO
  주제·청중·이 화면의 주된 일. 여기서 시각적 선택이 나온다.
  "8~11세 여아용 장난감"과 "재무 분석가용 대시보드"는 전혀 다르게 생겨야 한다.
  product.md §1~3 을 옮겨 적지 말고, 그것이 화면에 무엇을 요구하는지를 쓴다.
-->

## 2. 방향

<!-- TODO
  한두 문장. 무엇을 기억하게 할 것인가.
  "정보 밀도가 높고 조용하다. 눈에 띄는 것은 예매 상태 하나뿐이다." 처럼.
-->

## 3. UI 라이브러리

| | |
|---|---|
| 라이브러리 | <!-- TODO: 예) MUI v9 — 근거 ADR-000X --> |
| 기본값을 그대로 쓰는 범위 | <!-- TODO: 예) 컴포넌트 형태·간격은 기본값. 색·타이포는 정한다 --> |
| 직접 정하는 것 | <!-- TODO --> |

**라이브러리를 고르면 디자인 결정의 대부분이 따라온다.** 남는 것만 여기서 정한다.

## 4. 토큰

정본은 `docs/product/design-tokens.css` 다. **이 문서는 왜 그렇게 정했는지만 적는다.**

| 축 | 결정 | 왜 |
|---|---|---|
| 색 | <!-- TODO --> | |
| 타이포 | <!-- TODO --> | |
| 간격 | <!-- TODO --> | |

<!--
  ★ 한국어 UI 라면 서체에 한글 글리프가 있는지 반드시 확인한다.
    Inter, Playfair Display, Space Grotesk 등 흔히 추천되는 서체는 한글이 없어
    시스템 폰트로 폴백되고, 그러면 여기서 정한 것이 화면에 나타나지 않는다.
    Pretendard, Noto Sans KR, Gothic A1 등을 쓴다.
-->

## 5. 의미 매핑

같은 뜻은 어디서나 같은 색·같은 말이어야 한다. **여기가 정본이다.**

| 의미 | 색 역할 | 화면에 쓰는 말 |
|---|---|---|
| <!-- TODO: 예) 예매 가능 --> | <!-- success --> | <!-- 예매가능 --> |

컴포넌트 안에만 있고 여기 없으면, 다음 화면에서 다른 값이 쓰인다.

## 6. 하지 않는 것

<!-- TODO
  기본값이라서 나오는 것들. frontend-design 스킬이 열거한 것 중 이 제품에 해당하는 것.
  예) 카드마다 hover 트랜지션, 섹션마다 fade-and-slide-up, 제목 한 단어만 강조,
      라벨 ALL-CAPS, 버튼 텍스트 뒤 '→', 의미 없는 01/02/03 번호
-->

## 7. 품질 하한선

이건 취향이 아니라 하한선이다. Phase 4 에서 확인한다.

- [ ] 모바일까지 반응형
- [ ] 키보드 포커스가 눈에 보인다
- [ ] `prefers-reduced-motion` 을 존중한다
- [ ] 본문 대비 4.5:1 이상
- [ ] `<html lang>` 이 실제 콘텐츠 언어와 같다
- [ ] 라이브러리가 요구하는 서체가 실제로 로드된다
