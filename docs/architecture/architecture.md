# 아키텍처

<!--
  시스템이 어떻게 구성되는지를 산문으로 쓴다. Phase 1(설계)이 이 문서를 읽고
  "이 코드를 어느 계층에 둘 것인가"를 결정한다.

  ★ 기계가 검사할 수 있는 제약은 여기 말고 constraints.yaml 에 쓴다.
    이 문서에 "API 는 리포지토리를 직접 부르지 않는다"라고 적어두면 아무도 검사하지 않는다.
    constraints.yaml 에 쓰면 Phase 4 가 실제로 확인한다.

  /wf-init 또는 architecture-doc 스킬로 작성한다.
-->

**최종 갱신**: <!-- TODO: YYYY-MM-DD -->

---

## 1. 전체 구조

<!--
  ASCII 다이어그램. 무엇이 무엇을 호출하는지 화살표 방향이 보이게 그린다.
-->

```
  <TODO: 구성 요소와 호출 방향>

  예)
  ┌──────────┐     ┌──────────┐     ┌──────────────┐     ┌────┐
  │  Client  │────►│   API    │────►│   Service    │────►│ DB │
  └──────────┘     └──────────┘     └──────┬───────┘     └────┘
                                           │
                                    ┌──────▼───────┐
                                    │  Repository  │
                                    └──────────────┘
```

## 2. 계층

<!--
  각 계층이 무엇을 책임지고 무엇을 하지 않는지.
  "하지 않는 것"이 계층 경계를 만든다.
-->

| 계층 | 책임 | 하지 않는 것 | 위치 |
|---|---|---|---|
| <!-- TODO --> | | | |

## 3. 의존 방향

<!--
  어느 방향의 호출이 허용되고 금지되는가.
  여기서 정한 것 중 검사 가능한 것을 constraints.yaml 로 옮긴다.
-->

```
허용:  API ──► Service ──► Repository ──► DB
금지:  API ──╳─► Repository        (트랜잭션 경계가 흐려진다)
       Service ──╳─► API           (역방향 의존)
```

## 4. 데이터 흐름

<!-- TODO: 주요 흐름 1~3개. 읽기 경로와 쓰기 경로를 구분해서. -->

## 5. 외부 의존

| 대상 | 용도 | 실패하면 | 대안 |
|---|---|---|---|
| <!-- TODO --> | | | |

## 6. 이 구조를 고른 이유

<!--
  큰 결정은 ADR 로 빼고 여기서는 링크만 남긴다.
  여기 산문으로 길게 쓰면 나중에 "왜?"를 검색할 수 없다.
-->

- 계층 분리 → [ADR-0002](../decisions/ADR-0002-....md)

## 7. 알려진 부채

<!--
  현재 구조가 이상적이지 않은 지점과 그 이유. 숨기지 않는다.
  Phase 5 회고에서 여기에 항목이 추가될 수 있다.
-->

| 무엇 | 왜 이대로 두는가 | 언제 다시 볼 것인가 |
|---|---|---|
| | | |
