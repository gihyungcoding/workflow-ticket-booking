{
  "task_id": "TASK-001",
  "route": "Backend",
  "route_reason": "performance 테이블 신설, 예매 상태 계산 로직, GET 엔드포인트 2개가 모두 서버 범위이며 프론트는 응답의 status 값을 그대로 배지로 표시만 한다 (TASK-002는 별도 태스크)",

  "design": {
    "inputs": [
      {
        "name": "id",
        "type": "Long",
        "source": "경로 변수 (GET /api/performances/{id})",
        "constraints": ["필수", "양의 정수"]
      },
      {
        "name": "status",
        "type": "String (enum)",
        "source": "쿼리 파라미터 (GET /api/performances)",
        "constraints": ["선택", "UPCOMING|OPEN|SOLD_OUT|CLOSED|CANCELLED 중 하나"]
      },
      {
        "name": "page",
        "type": "int",
        "source": "쿼리 파라미터",
        "constraints": ["선택", "기본값 0", "0 이상"]
      },
      {
        "name": "size",
        "type": "int",
        "source": "쿼리 파라미터",
        "constraints": ["선택", "기본값 20", "1~100"]
      },
      {
        "name": "Performance 엔티티 필드값",
        "type": "domain object (title/venue/startAt/openAt/closeAt/totalSeats/availableSeats/cancelled)",
        "source": "영속성 계층 경계 — API 요청이 아니라 저장을 시도하는 호출자(Repository.save 등)가 주는 값 (F7)",
        "constraints": ["title/venue/startAt/openAt/closeAt/totalSeats/availableSeats 는 NOT NULL", "total_seats > 0", "0 <= available_seats <= total_seats"]
      }
    ],
    "outputs": [
      {
        "name": "PerformanceListResponse",
        "type": "object",
        "description": "content(PerformanceResponse[]) / page / size / totalElements 4개 필드만 갖는 커스텀 응답 — Spring Data Page의 기본 직렬화(pageable, totalPages, number 등)를 그대로 노출하지 않는다"
      },
      {
        "name": "PerformanceResponse",
        "type": "object",
        "description": "id, title, venue, startAt, openAt, closeAt, totalSeats, availableSeats, status — status는 저장된 컬럼이 아니라 요청 시점에 계산되어 채워진다"
      },
      {
        "name": "404 PERFORMANCE_NOT_FOUND",
        "type": "error",
        "description": "존재하지 않는 id로 상세 조회 시 {code: PERFORMANCE_NOT_FOUND, message}"
      },
      {
        "name": "제약 위반 저장 거부",
        "type": "error (persistence, F7)",
        "description": "NOT NULL/CHECK 제약을 어긴 Performance 를 저장(flush 포함)하면 예외가 발생해 거부된다 — 이건 HTTP 응답 계약이 아니라 F7이 검증하는 영속성 계층 경계의 출력이다"
      }
    ],
    "flows": [
      {
        "id": "F1",
        "name": "목록 조회 및 페이지네이션",
        "steps": [
          "컨트롤러가 status/page/size 파싱 (size 기본 20, 최대 100)",
          "Repository가 start_at >= now 조건으로 페이지네이션 조회 (status 파라미터 없으면 이 조건만)",
          "각 행에 대해 서비스가 status 계산 후 PerformanceResponse로 매핑",
          "PerformanceListResponse(content/page/size/totalElements)로 래핑해 200 반환"
        ],
        "covers": [
          "GET /api/performances 호출 시 200과 함께 content/page/size/totalElements 형식으로 목록이 반환된다"
        ]
      },
      {
        "id": "F2",
        "name": "예매 상태 계산 규칙",
        "steps": [
          "cancelled == true 이면 다른 조건과 무관하게 CANCELLED (최우선)",
          "now < openAt 이면 UPCOMING",
          "openAt <= now <= closeAt 이고 availableSeats > 0 이면 OPEN",
          "openAt <= now <= closeAt 이고 availableSeats == 0 이면 SOLD_OUT",
          "now > closeAt 이면 CLOSED"
        ],
        "covers": [
          "availableSeats == 0 인 공연은 status: SOLD_OUT 으로 반환된다",
          "cancelled == true 인 공연은 시각·좌석 수와 무관하게 status: CANCELLED 로 반환된다"
        ]
      },
      {
        "id": "F3",
        "name": "status 쿼리 파라미터 필터링 (DB 레벨)",
        "steps": [
          "status는 저장 컬럼이 아니므로 F2의 5개 규칙을 Repository WHERE 절 조건으로 그대로 변환 (예: OPEN → NOT cancelled AND open_at <= :now AND :now <= close_at AND available_seats > 0)",
          "이 필터는 start_at >= now 조건과 AND로 결합해 적용",
          "totalElements/페이지네이션은 필터 적용 후 개수를 기준으로 계산 — 애플리케이션에서 전체를 읽어 메모리 필터링하지 않는다"
        ],
        "covers": [
          "status=OPEN 필터 시 openAt <= now <= closeAt 이고 availableSeats > 0 인 공연만 반환된다"
        ]
      },
      {
        "id": "F4",
        "name": "지난 공연 제외",
        "steps": [
          "목록 조회 쿼리는 상태 필터 유무와 무관하게 항상 start_at >= now 조건을 포함",
          "status=CLOSED 필터는 '조기 마감'을 포함하므로 start_at >= now 인 공연 중에서도 걸릴 수 있다 (별개 조건)"
        ],
        "covers": [
          "start_at < now 인 공연은 상태 필터와 무관하게 목록에서 제외된다"
        ]
      },
      {
        "id": "F5",
        "name": "상세 조회 및 404",
        "steps": [
          "id로 단건 조회",
          "없으면 PerformanceNotFoundException 발생 (서비스 계층)",
          "컨트롤러/예외 핸들러가 404 + {code: PERFORMANCE_NOT_FOUND, message} 로 변환"
        ],
        "covers": [
          "존재하지 않는 id 로 GET /api/performances/{id} 호출 시 404와 PERFORMANCE_NOT_FOUND 코드가 반환된다"
        ]
      },
      {
        "id": "F6",
        "name": "Clock 기반 경계 판정",
        "steps": [
          "PerformanceService는 java.time.Clock을 주입받아 clock.instant()로만 now를 구한다 (Instant.now()/LocalDateTime.now() 직접 호출 금지, ADR-0005)",
          "Repository 쿼리의 now 파라미터도 서비스가 구한 동일한 now를 바인드한다 (SQL now() 미사용)",
          "테스트는 Clock.fixed(오픈 정각 / 마감 1초 전)로 대체해 경계값을 결정적으로 검증"
        ],
        "covers": [
          "Clock 을 오픈 정각·마감 1초 전으로 고정한 테스트에서 경계값이 올바른 status 를 반환한다 (SQL now() 미사용을 리뷰로 확인)"
        ]
      },
      {
        "id": "F7",
        "name": "엔티티 제약과 마이그레이션 제약의 일치",
        "steps": [
          "V1__create_performance.sql의 NOT NULL/CHECK 제약(title/venue/startAt/openAt/closeAt/totalSeats/availableSeats NOT NULL, total_seats > 0, 0 <= available_seats <= total_seats)을 Performance 엔티티 애노테이션으로도 표현한다",
          "테스트는 H2 + ddl-auto=create-drop로 엔티티 애노테이션에서 스키마를 만들므로(ADR-0006), 애노테이션이 마이그레이션과 어긋나면 이 스키마 자체가 마이그레이션과 달라진다 — @DataJpaTest로 제약 위반 시 저장이 거부되는지 검증해 이 어긋남을 방지한다",
          "예: availableSeats > totalSeats 로 저장을 시도하면 예외가 발생해야 한다"
        ],
        "covers": [
          "performance 테이블의 NOT NULL/CHECK 제약이 엔티티 애노테이션으로도 표현되어 있고, 위반 시 저장이 거부된다"
        ]
      }
    ]
  },

  "architecture_refs": {
    "layer": "API + Service + Repository (읽기 전용 요청이 3계층을 그대로 통과)",
    "note": "의존 방향은 API → Service → Repository → DB 만 허용 (architecture.md §3). PerformanceNotFoundException은 Service에서 던지고 API 계층에서 404로 변환 — 이건 '응답 변환' 책임이라 API 계층 역할과 충돌하지 않는다.",
    "constraints_applied": ["ARCH-001", "ARCH-002"],
    "note_on_constraints_yaml": "constraints.yaml의 ARCH-001/002는 glob이 backend/.../controller/**를 검사하는데, architecture.md §2 표는 API 계층 위치를 backend/.../api 로 명시하고 이번 태스크도 그 경로(PerformanceController.java in .../api/)를 따른다. 즉 실제 신규 파일이 이 계층에 생기면 현재 glob으로는 매치되지 않아 ARCH-001/002가 사실상 검사되지 않는 상태가 된다. Phase 4 전에 glob을 **/api/**로 정정하는 것을 권장 — Phase 1 EXIT GATE와는 무관해 여기서는 보고만 하고 constraints.yaml은 고치지 않았다."
  },
  "adr_refs": ["ADR-0003", "ADR-0005", "ADR-0006"],

  "codebase_analysis": {
    "target_files": [
      { "path": "backend/src/main/resources/db/migration/V1__create_performance.sql", "change": "신규 생성 — performance 테이블 마이그레이션 (기능 문서 §2 SQL 그대로)" },
      { "path": "backend/src/main/java/com/example/ticket_booking/domain/Performance.java", "change": "신규 생성 — JPA 엔티티 (id/title/venue/startAt/openAt/closeAt/totalSeats/availableSeats/cancelled/createdAt), status는 필드로 두지 않는다. V1 마이그레이션의 NOT NULL(필수 필드 전부)과 CHECK(total_seats > 0, 0<=available_seats<=total_seats)를 @Column(nullable=false)/@Check 등 애노테이션으로도 표현한다 (F7)" },
      { "path": "backend/src/main/java/com/example/ticket_booking/repository/PerformanceRepository.java", "change": "신규 생성 — JpaRepository<Performance, Long> + start_at>=now 페이지네이션 조회 + status별 WHERE 조건 쿼리 (F3)" },
      { "path": "backend/src/main/java/com/example/ticket_booking/service/PerformanceStatus.java", "change": "신규 생성 — UPCOMING/OPEN/SOLD_OUT/CLOSED/CANCELLED enum" },
      { "path": "backend/src/main/java/com/example/ticket_booking/service/PerformanceService.java", "change": "신규 생성 — Clock 주입, F2 상태 계산, DTO 매핑, 페이지네이션·필터 위임" },
      { "path": "backend/src/main/java/com/example/ticket_booking/service/PerformanceNotFoundException.java", "change": "신규 생성 — 상세 조회 실패 시 서비스가 던지는 예외" },
      { "path": "backend/src/main/java/com/example/ticket_booking/api/PerformanceController.java", "change": "신규 생성 — GET /api/performances, GET /api/performances/{id}" },
      { "path": "backend/src/main/java/com/example/ticket_booking/api/PerformanceExceptionHandler.java", "change": "신규 생성 — @RestControllerAdvice, PerformanceNotFoundException → 404 + PERFORMANCE_NOT_FOUND 매핑" },
      { "path": "backend/src/main/java/com/example/ticket_booking/api/dto/PerformanceResponse.java", "change": "신규 생성" },
      { "path": "backend/src/main/java/com/example/ticket_booking/api/dto/PerformanceListResponse.java", "change": "신규 생성" },
      { "path": "backend/src/main/java/com/example/ticket_booking/api/dto/ErrorResponse.java", "change": "신규 생성 — {code, message}" },
      { "path": "backend/src/main/java/com/example/ticket_booking/config/ClockConfig.java", "change": "신규 생성 — @Bean Clock clock() { return Clock.systemUTC(); } (현재 프로젝트에 Clock 빈이 없어 새로 추가)" }
    ],
    "reusable": [],
    "existing_tests": [
      "backend/src/test/java/com/example/ticket_booking/TicketBookingApplicationTests.java — @SpringBootTest 컨텍스트 로딩 테스트, 패키지 컨벤션 참고용"
    ],
    "unresolved": [
      "실제 패키지 루트는 com.example.ticket_booking 이다 (build.gradle group='com.example', 기존 TicketBookingApplication.java 위치 확인). 기능 문서 §5와 tasks.json의 implementation_spec.paths는 com.ticketbooking을 쓰고 있어 실제 코드베이스와 다르다 — 이 Plan은 실재하는 com.example.ticket_booking을 기준으로 경로를 정정했다.",
      "prod용 PostgreSQL datasource(application.properties/application-prod.properties 등) 설정이 아직 없다. 이 태스크의 acceptance_criteria는 테스트(H2)로 검증되므로 범위에 포함하지 않았으나, 실제 배포 전에는 별도로 필요하다.",
      "테스트는 spring.jpa.hibernate.ddl-auto=create-drop + spring.flyway.enabled=false (H2)로 엔티티 애노테이션이 스키마를 만든다 — V1 마이그레이션 SQL 자체는 여전히 테스트에서 실행되지 않는다(ADR-0006, 임시 조치). Phase 2a에서 F7/AC8을 추가해 엔티티 애노테이션이 마이그레이션과 같은 제약을 표현하고 위반 시 거부됨은 @DataJpaTest로 검증하기로 했다 — 다만 이는 '애노테이션이 제약을 표현하는지'를 보장할 뿐 'SQL 파일 자체가 문법적으로 유효한지'는 여전히 검증하지 않는다."
    ]
  },

  "acceptance_criteria": [
    "GET /api/performances 호출 시 200과 함께 content/page/size/totalElements 형식으로 목록이 반환된다",
    "status=OPEN 필터 시 openAt <= now <= closeAt 이고 availableSeats > 0 인 공연만 반환된다",
    "availableSeats == 0 인 공연은 status: SOLD_OUT 으로 반환된다",
    "cancelled == true 인 공연은 시각·좌석 수와 무관하게 status: CANCELLED 로 반환된다",
    "start_at < now 인 공연은 상태 필터와 무관하게 목록에서 제외된다",
    "존재하지 않는 id 로 GET /api/performances/{id} 호출 시 404와 PERFORMANCE_NOT_FOUND 코드가 반환된다",
    "Clock 을 오픈 정각·마감 1초 전으로 고정한 테스트에서 경계값이 올바른 status 를 반환한다 (SQL now() 미사용을 리뷰로 확인)",
    "performance 테이블의 NOT NULL/CHECK 제약이 엔티티 애노테이션으로도 표현되어 있고, 위반 시 저장이 거부된다"
  ],

  "test_hints": {
    "framework": "JUnit5 + Spring Boot Test (H2, ADR-0006)",
    "command": "./gradlew test",
    "mock_strategy": "PerformanceServiceTest는 PerformanceRepository를 스텁/모킹하고 Clock.fixed(...)로 경계 시각 고정 (단위). PerformanceRepositoryTest는 @DataJpaTest + H2로 F3의 상태별 WHERE 조건과 start_at>=now 조건을 실제 쿼리로 검증하고(통합), F7의 NOT NULL/CHECK 제약 위반 시 저장이 거부되는지도 같은 @DataJpaTest에서 확인한다. PerformanceControllerTest는 @WebMvcTest + 모킹된 PerformanceService로 요청 파싱·응답 포맷·404 매핑을 검증."
  },

  "amendments": [
    {
      "when": "Phase 2a (시나리오 보완, HITL#1 이전)",
      "what": "F7 flow와 AC8(엔티티 제약-마이그레이션 제약 일치) 추가",
      "why": "사용자가 HITL#1에서 unresolved 항목(엔티티 애노테이션과 V1 마이그레이션 제약의 수동 동기화 리스크)을 실제 테스트로 보호하자고 요청",
      "reference": "memory-bank/TASK-001/checkpoints/phase2a/CP-2.2_canonical-scenarios.md"
    }
  ]
}
